version: '2'
services:
  simulador-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Simulador/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Simulador
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
